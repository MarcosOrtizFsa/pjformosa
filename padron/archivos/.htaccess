# Los CSV de importación se procesan desde PHP y nunca deben descargarse por HTTP.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
    Deny from all
</IfModule>
